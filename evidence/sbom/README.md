# SBOM Evidence

Run `make sbom-python` for a CycloneDX Python SBOM. Use Syft for the built container image. Review component identity, version, source, license, known vulnerabilities, maintenance and provenance; inventory alone does not establish trust.
