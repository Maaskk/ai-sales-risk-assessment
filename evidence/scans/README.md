# Scan Evidence

Run `make scan-python` for Bandit and pip-audit JSON. Run `scripts/run_container_evidence.sh` or the GitHub security workflow for Trivy image results. Findings require human triage; a zero count is not proof of security.
