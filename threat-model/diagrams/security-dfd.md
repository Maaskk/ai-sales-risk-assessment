# Security DFD

The canonical text DFD is maintained in [`../../docs/data-flow.md`](../../docs/data-flow.md). Keep this pointer so the threat-model directory remains navigable without duplicating an independently drifting diagram.
